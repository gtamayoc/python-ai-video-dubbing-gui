# Core pipeline
faster-whisper>=1.0.0
argostranslate>=1.9.0
edge-tts>=6.1.0
imageio-ffmpeg
moviepy>=1.0.3
pydub
langdetect
yt-dlp


# LLM translation backend – GGUF is the fastest (recommended)
# On CPU-only machines install without GPU flags:
#   pip install llama-cpp-python
# On machines with CUDA:
#   CMAKE_ARGS="-DLLAMA_CUDA=on" pip install llama-cpp-python
llama-cpp-python

# Transformers fallback (used only if llama-cpp-python is unavailable)
transformers>=4.40.0
torch

# GUI
customtkinter
tkinterdnd2
Pillow

# GPU acceleration (optional — CPU fallback automatic)
# nvidia-cublas-cu12   # uncomment for CUDA 12
# nvidia-cudnn-cu12    # uncomment for CUDA 12

# Profiling & logging
# rich                 # optional: better console output
python-dotenv

# Optional: real multi-speaker diarization
# Requires: export HF_TOKEN=<your_huggingface_token>
pyannote.audio
onnxruntime
